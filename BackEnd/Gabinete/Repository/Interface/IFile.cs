using System;
using System.Collections.Generic;
using System.Text;

namespace Repository.Interface
{
    public interface IFile
    {
        /*
           Task<ServiceResult> UploadFile(IFormFile file, FilePickViewModel mod, int id);
        Task<ServiceResult> UploadAlbum(IFormFile file, FilePickViewModel mod, int id);
        ServiceResult GetAlbum(int id);
         */
    }
}
